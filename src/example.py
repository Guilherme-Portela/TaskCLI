df = {
    "tasks": {
        "0": "Clean my Room",
        "1": "Buy food",
        "2": "Buy Victoria girlfriend's day gift"
    }
}

# How i can get a new ID for a new task?
# A)
    # ids = len(df["tasks"])
    # id = ids + 1
# B)
    # id = len(df["tasks"])
# William: I think... B